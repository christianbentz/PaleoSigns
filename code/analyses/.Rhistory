sign.data$coding_clean <- coding.clean
# again remove rows for which there is no sign coding (after cleaning)
sign.data <- sign.data[!sign.data$coding_clean == "", ]
sign.data <- read.csv(
"~/Github/UpperPalCombinatorics/data/signBase/Sb2Artifact-2024-02-20.csv")
# remove certain columns
sign.data <- subset(sign.data, select = -c(id, comments, short_description, references))
# remove rows for which there is no sign coding
sign.data <- sign.data[!sign.data$sign_coding == "", ]
View(sign.data)
coding.clean <- gsub('[!?]', '', sign.data$sign_coding)
# remove sign strings which are not discrete, i.e. overlapping sign types
# -> indicated by parentheses '()'
coding.clean <- gsub("\\([^()]*\\)", "", coding.clean)
# remove sign strings which are not in linear order, i.e. those grouped in visual fields
# -> indicated by square brackets '[]'
coding.clean <- gsub("\\[(.*?)\\]", "", coding.clean)
# replace multiple underscores by single underscore
coding.clean <- gsub("( _ )+", " _ ", coding.clean)
# trim the underscores and white spaces from beginning and end of sequences
coding.clean <- trimws(coding.clean, which = "both", whitespace = "[ \\_]")
# add column to original data frame again
sign.data$coding_clean <- coding.clean
# again remove rows for which there is no sign coding (after cleaning)
sign.data <- sign.data[!sign.data$coding_clean == "", ]
View(sign.data)
sign.data <- sign.data[nchar(sign.data$coding_clean) > 9, ]
View(sign.data)
sign.data <- read.csv(
"~/Github/UpperPalCombinatorics/data/signBase/Sb2Artifact-2024-02-20.csv")
# remove certain columns
sign.data <- subset(sign.data, select = -c(id, comments, short_description, references))
# remove rows for which there is no sign coding
sign.data <- sign.data[!sign.data$sign_coding == "", ]
coding.clean <- gsub('[!?]', '', sign.data$sign_coding)
# remove sign strings which are not discrete, i.e. overlapping sign types
# -> indicated by parentheses '()'
coding.clean <- gsub("\\([^()]*\\)", "", coding.clean)
# remove sign strings which are not in linear order, i.e. those grouped in visual fields
# -> indicated by square brackets '[]'
coding.clean <- gsub("\\[(.*?)\\]", "", coding.clean)
# replace multiple underscores by single underscore
coding.clean <- gsub("( _ )+", " _ ", coding.clean)
# trim the underscores and white spaces from beginning and end of sequences
coding.clean <- trimws(coding.clean, which = "both", whitespace = "[ \\_]")
# add column to original data frame again
sign.data$coding_clean <- coding.clean
# again remove rows for which there is no sign coding (after cleaning)
sign.data <- sign.data[!sign.data$coding_clean == "", ]
sign.data <- sign.data[nchar(sign.data$coding_clean) > 4, ]
View(sign.data)
knitr::opts_chunk$set(echo = TRUE)
set.seed(15022018)
teddi.sample <-sample_n(teddi.data, 1000)
line.data <- read.csv(
"~/Github/UpperPalCombinatorics/data/TeDDi/line_sample.csv")
file.data <- read.csv("~/Data/TeDDi_dumps/file.csv")
# merge the files
teddi.data <- merge(line.data, file.data, by.x = "file_id", by.y = "id")
set.seed(15022018)
teddi.sample <-sample_n(teddi.data, 1000)
teddi.clean <- teddi.sample[nchar(teddi.sample$text) > 4, ]
nrow(teddi.sample)
# unique ISO codes (i.e. languages)
unique(teddi.sample$iso639_3)
# writing systems
unique(teddi.sample$writing_system)
write.csv(teddi.clean,
file = "~/Github/UpperPalCombinatorics/data/TeDDi/final_sample.csv", row.names = F)
knitr::opts_chunk$set(echo = TRUE)
library(Hmisc)
library(stringr)
library(readr)
period <- "UrukIII"
# transliteration as text file with line breaks
translit.file = paste("~/Github/UpperPalCombinatorics/data/CDLI/",
paste(period, "/inscriptions.atf", sep = ""),
sep ="")
textlines <- scan(translit.file, what = "char", quote = "", comment.char = "",
encoding = "UTF-8", sep = "\n" , skip = 0)
# load transliteration as single character string
textstring <- read_file(translit.file)
library(Hmisc)
library(stringr)
library(readr)
install.packages("readr")
install.packages("readr")
knitr::opts_chunk$set(echo = TRUE)
library(Hmisc)
library(stringr)
library(readr)
period <- "UrukIII"
# transliteration as text file with line breaks
translit.file = paste("~/Github/UpperPalCombinatorics/data/CDLI/",
paste(period, "/inscriptions.atf", sep = ""),
sep ="")
textlines <- scan(translit.file, what = "char", quote = "", comment.char = "",
encoding = "UTF-8", sep = "\n" , skip = 0)
# load transliteration as single character string
textstring <- read_file(translit.file)
# load sign list
signlist.file = paste("~/Github/UpperPalCombinatorics/data/CDLI/",
paste(period, "/signlist.txt", sep = ""),
sep ="")
signlist <- scan(signlist.file, what = "char", quote = "", comment.char = "",
encoding = "UTF-8", sep = "\n" , skip = 0)
tablet.id <- textlines[grep(pattern = "\\&P", textlines)]
# check length
length(tablet.id)
# remove ampersand
tablet.id <- gsub(pattern = "\\&", "", tablet.id)
# get just the ID
tablet.id <- substr(tablet.id, 1, 7)
textchunks <- str_split(textstring, pattern = "\\&P")
# unlist and remove first element (empty)
textchunks <- unlist(textchunks)[-1]
# remove the header
textchunks <- gsub("\\=.*qpc", "", textchunks)
# use signlist provided by CDLI to extract only signs
# create a duplicate signlist with `#' following after the signs,
# this means the sign is partly broken but identifiable
signlist.hashtag <- paste0(signlist,'#')
# concatenate the two lists (i.e. vectors)
signlist <- c(signlist, signlist.hashtag)
# first some characters in the sign list need to be escaped
signlist <- escapeRegex(signlist)
# add white spaces after character strings in signlist
# this helps with only extracting full string matches
signlist <- paste0(signlist,' ')
# remove 'X' as this is a placeholder for unidentified signs
signlist <- signlist[!signlist == 'X ']
# run for loop to extract signs based on sign list
# initialize vectors
signs.orig.vec <- c()
signs.expand.vec <- c()
for (i in 1:length(textchunks)){
# note that the order of signs is here sometimes changed,
# as it follows the order of signs in sign list
signs.orig <- unlist(str_extract_all(textchunks[i], paste(signlist, collapse = "|")))
signs <- unlist(str_extract_all(textchunks[i], paste(signlist, collapse = "|")))
# trim white spaces around signs
signs.orig <- trimws(signs.orig)
signs <- trimws(signs)
# remove the hashtag
signs <- gsub("#", "", signs)
# unravel the multiplication syntax to get multiple sign tokens
try({ # if the processing fails for a certain row, there will be no output for this row,
# but the try() function allows the loop to keep running
for (j in 1:length(signs)){
sign.syntax <- signs[j]
# get only number
remainder <- sub(pattern = "\\(.*\\)", "", sign.syntax)
# if number then multiply with sign type, else
# leave sign syntax as is
if (!grepl("[^0-9]", remainder)){
# get sign.type inside round brackets
sign.type <- gsub(".*\\((.*)\\).*", "\\1", sign.syntax)
# repeat given the number ("remainder" from above)
sign.tokens <- rep(sign.type, remainder)
# collaps into character string
sign.tokens <- paste(sign.tokens, collapse = " ")
} else {
sign.tokens <- sign.syntax
}
signs[j] <- sign.tokens
}}) # end of try() function
# collapse the signs into one string
signs <- paste(signs, collapse = " _ ")
signs.orig <- paste(signs.orig, collapse = " _ ")
signs.orig.vec <- c(signs.orig.vec, signs.orig)
signs.expand.vec <- c(signs.expand.vec, signs)
}
# compile final data frame
df <- cbind(tablet.id, signs.orig.vec, signs.expand.vec)
head(df)
View(df)
period <- "UrukIII"
# transliteration as text file with line breaks
translit.file = paste("~/Github/UpperPalCombinatorics/data/CDLI/",
paste(period, "/inscriptions.atf", sep = ""),
sep ="")
textlines <- scan(translit.file, what = "char", quote = "", comment.char = "",
encoding = "UTF-8", sep = "\n" , skip = 0)
# load transliteration as single character string
textstring <- read_file(translit.file)
# load sign list
signlist.file = paste("~/Github/UpperPalCombinatorics/data/CDLI/",
paste(period, "/signlist.txt", sep = ""),
sep ="")
signlist <- scan(signlist.file, what = "char", quote = "", comment.char = "",
encoding = "UTF-8", sep = "\n" , skip = 0)
tablet.id <- textlines[grep(pattern = "\\&P", textlines)]
# check length
length(tablet.id)
# remove ampersand
tablet.id <- gsub(pattern = "\\&", "", tablet.id)
# get just the ID
tablet.id <- substr(tablet.id, 1, 7)
textchunks <- str_split(textstring, pattern = "\\&P")
# unlist and remove first element (empty)
textchunks <- unlist(textchunks)[-1]
# remove the header
textchunks <- gsub("\\=.*qpc", "", textchunks)
# use signlist provided by CDLI to extract only signs
# create a duplicate signlist with `#' following after the signs,
# this means the sign is partly broken but identifiable
signlist.hashtag <- paste0(signlist,'#')
# concatenate the two lists (i.e. vectors)
signlist <- c(signlist, signlist.hashtag)
# first some characters in the sign list need to be escaped
signlist <- escapeRegex(signlist)
# add white spaces after character strings in signlist
# this helps with only extracting full string matches
signlist <- paste0(signlist,' ')
# remove 'X' as this is a placeholder for unidentified signs
signlist <- signlist[!signlist == 'X ']
# run for loop to extract signs based on sign list
# initialize vectors
signs.orig.vec <- c()
signs.expand.vec <- c()
for (i in 1:length(textchunks)){
# note that the order of signs is here sometimes changed,
# as it follows the order of signs in sign list
signs.orig <- unlist(str_extract_all(textchunks[i], paste(signlist, collapse = "|")))
signs <- unlist(str_extract_all(textchunks[i], paste(signlist, collapse = "|")))
# trim white spaces around signs
signs.orig <- trimws(signs.orig)
signs <- trimws(signs)
# remove the hashtag
signs <- gsub("#", "", signs)
# unravel the multiplication syntax to get multiple sign tokens
try({ # if the processing fails for a certain row, there will be no output for this row,
# but the try() function allows the loop to keep running
for (j in 1:length(signs)){
sign.syntax <- signs[j]
# get only number
remainder <- sub(pattern = "\\(.*\\)", "", sign.syntax)
# if number then multiply with sign type, else
# leave sign syntax as is
if (!grepl("[^0-9]", remainder)){
# get sign.type inside round brackets
sign.type <- gsub(".*\\((.*)\\).*", "\\1", sign.syntax)
# repeat given the number ("remainder" from above)
sign.tokens <- rep(sign.type, remainder)
# collaps into character string
sign.tokens <- paste(sign.tokens, collapse = " ")
} else {
sign.tokens <- sign.syntax
}
signs[j] <- sign.tokens
}}) # end of try() function
# collapse the signs into one string
signs <- paste(signs, collapse = " _ ")
signs.orig <- paste(signs.orig, collapse = " _ ")
signs.orig.vec <- c(signs.orig.vec, signs.orig)
signs.expand.vec <- c(signs.expand.vec, signs)
}
# compile final data frame
cunei.data <- cbind(tablet.id, signs.orig.vec, signs.expand.vec)
head(cunei.data)
View(cunei.data)
cunei.data <- cunei.data[-which(cunei.data$signs.orig.vec == ""), ]
cunei.data <- data.frame(tablet.id, signs.orig.vec, signs.expand.vec)
View(cunei.data)
cunei.data <- cunei.data[-which(cunei.data$signs.orig.vec == ""), ]
cunei.data <- cunei.data[-which(cunei.data$signs.orig.vec == ""), ]
period <- "UrukIII"
# transliteration as text file with line breaks
translit.file = paste("~/Github/UpperPalCombinatorics/data/CDLI/",
paste(period, "/inscriptions.atf", sep = ""),
sep ="")
textlines <- scan(translit.file, what = "char", quote = "", comment.char = "",
encoding = "UTF-8", sep = "\n" , skip = 0)
# load transliteration as single character string
textstring <- read_file(translit.file)
# load sign list
signlist.file = paste("~/Github/UpperPalCombinatorics/data/CDLI/",
paste(period, "/signlist.txt", sep = ""),
sep ="")
signlist <- scan(signlist.file, what = "char", quote = "", comment.char = "",
encoding = "UTF-8", sep = "\n" , skip = 0)
tablet.id <- textlines[grep(pattern = "\\&P", textlines)]
# check length
length(tablet.id)
# remove ampersand
tablet.id <- gsub(pattern = "\\&", "", tablet.id)
# get just the ID
tablet.id <- substr(tablet.id, 1, 7)
textchunks <- str_split(textstring, pattern = "\\&P")
# unlist and remove first element (empty)
textchunks <- unlist(textchunks)[-1]
# remove the header
textchunks <- gsub("\\=.*qpc", "", textchunks)
# use signlist provided by CDLI to extract only signs
# create a duplicate signlist with `#' following after the signs,
# this means the sign is partly broken but identifiable
signlist.hashtag <- paste0(signlist,'#')
# concatenate the two lists (i.e. vectors)
signlist <- c(signlist, signlist.hashtag)
# first some characters in the sign list need to be escaped
signlist <- escapeRegex(signlist)
# add white spaces after character strings in signlist
# this helps with only extracting full string matches
signlist <- paste0(signlist,' ')
# remove 'X' as this is a placeholder for unidentified signs
signlist <- signlist[!signlist == 'X ']
# run for loop to extract signs based on sign list
# initialize vectors
signs.orig.vec <- c()
signs.expand.vec <- c()
for (i in 1:length(textchunks)){
# note that the order of signs is here sometimes changed,
# as it follows the order of signs in sign list
signs.orig <- unlist(str_extract_all(textchunks[i], paste(signlist, collapse = "|")))
signs <- unlist(str_extract_all(textchunks[i], paste(signlist, collapse = "|")))
# trim white spaces around signs
signs.orig <- trimws(signs.orig)
signs <- trimws(signs)
# remove the hashtag
signs <- gsub("#", "", signs)
# unravel the multiplication syntax to get multiple sign tokens
try({ # if the processing fails for a certain row, there will be no output for this row,
# but the try() function allows the loop to keep running
for (j in 1:length(signs)){
sign.syntax <- signs[j]
# get only number
remainder <- sub(pattern = "\\(.*\\)", "", sign.syntax)
# if number then multiply with sign type, else
# leave sign syntax as is
if (!grepl("[^0-9]", remainder)){
# get sign.type inside round brackets
sign.type <- gsub(".*\\((.*)\\).*", "\\1", sign.syntax)
# repeat given the number ("remainder" from above)
sign.tokens <- rep(sign.type, remainder)
# collaps into character string
sign.tokens <- paste(sign.tokens, collapse = " ")
} else {
sign.tokens <- sign.syntax
}
signs[j] <- sign.tokens
}}) # end of try() function
# collapse the signs into one string
signs <- paste(signs, collapse = " _ ")
signs.orig <- paste(signs.orig, collapse = " _ ")
signs.orig.vec <- c(signs.orig.vec, signs.orig)
signs.expand.vec <- c(signs.expand.vec, signs)
}
# compile final data frame
cunei.data <- data.frame(tablet.id, signs.orig.vec, signs.expand.vec)
head(cunei.data)
head(cunei.data)
cunei.data <- cunei.data[-which(cunei.data$signs.orig.vec == ""), ]
View(cunei.data)
write.csv(df, file = paste("~/Github/UpperPalCombinatorics/data/CDLI/",
paste(period, "/signs_clean.csv", sep = ""),
sep =""), row.names = F)
period <- "UrukIII"
# transliteration as text file with line breaks
translit.file = paste("~/Github/UpperPalCombinatorics/data/CDLI/",
paste(period, "/inscriptions.atf", sep = ""),
sep ="")
textlines <- scan(translit.file, what = "char", quote = "", comment.char = "",
encoding = "UTF-8", sep = "\n" , skip = 0)
# load transliteration as single character string
textstring <- read_file(translit.file)
# load sign list
signlist.file = paste("~/Github/UpperPalCombinatorics/data/CDLI/",
paste(period, "/signlist.txt", sep = ""),
sep ="")
signlist <- scan(signlist.file, what = "char", quote = "", comment.char = "",
encoding = "UTF-8", sep = "\n" , skip = 0)
tablet.id <- textlines[grep(pattern = "\\&P", textlines)]
# check length
length(tablet.id)
# remove ampersand
tablet.id <- gsub(pattern = "\\&", "", tablet.id)
# get just the ID
tablet.id <- substr(tablet.id, 1, 7)
period <- "UrukIII"
# transliteration as text file with line breaks
translit.file = paste("~/Github/UpperPalCombinatorics/data/CDLI/",
paste(period, "/inscriptions.atf", sep = ""),
sep ="")
textlines <- scan(translit.file, what = "char", quote = "", comment.char = "",
encoding = "UTF-8", sep = "\n" , skip = 0)
# load transliteration as single character string
textstring <- read_file(translit.file)
# load sign list
signlist.file = paste("~/Github/UpperPalCombinatorics/data/CDLI/",
paste(period, "/signlist.txt", sep = ""),
sep ="")
signlist <- scan(signlist.file, what = "char", quote = "", comment.char = "",
encoding = "UTF-8", sep = "\n" , skip = 0)
tablet.id <- textlines[grep(pattern = "\\&P", textlines)]
# check length
length(tablet.id)
# remove ampersand
tablet.id <- gsub(pattern = "\\&", "", tablet.id)
# get just the ID
tablet.id <- substr(tablet.id, 1, 7)
textchunks <- str_split(textstring, pattern = "\\&P")
# unlist and remove first element (empty)
textchunks <- unlist(textchunks)[-1]
# remove the header
textchunks <- gsub("\\=.*qpc", "", textchunks)
# use signlist provided by CDLI to extract only signs
# create a duplicate signlist with `#' following after the signs,
# this means the sign is partly broken but identifiable
signlist.hashtag <- paste0(signlist,'#')
# concatenate the two lists (i.e. vectors)
signlist <- c(signlist, signlist.hashtag)
# first some characters in the sign list need to be escaped
signlist <- escapeRegex(signlist)
# add white spaces after character strings in signlist
# this helps with only extracting full string matches
signlist <- paste0(signlist,' ')
# remove 'X' as this is a placeholder for unidentified signs
signlist <- signlist[!signlist == 'X ']
# run for loop to extract signs based on sign list
# initialize vectors
signs.orig.vec <- c()
signs.expand.vec <- c()
for (i in 1:length(textchunks)){
# note that the order of signs is here sometimes changed,
# as it follows the order of signs in sign list
signs.orig <- unlist(str_extract_all(textchunks[i], paste(signlist, collapse = "|")))
signs <- unlist(str_extract_all(textchunks[i], paste(signlist, collapse = "|")))
# trim white spaces around signs
signs.orig <- trimws(signs.orig)
signs <- trimws(signs)
# remove the hashtag
signs <- gsub("#", "", signs)
# unravel the multiplication syntax to get multiple sign tokens
try({ # if the processing fails for a certain row, there will be no output for this row,
# but the try() function allows the loop to keep running
for (j in 1:length(signs)){
sign.syntax <- signs[j]
# get only number
remainder <- sub(pattern = "\\(.*\\)", "", sign.syntax)
# if number then multiply with sign type, else
# leave sign syntax as is
if (!grepl("[^0-9]", remainder)){
# get sign.type inside round brackets
sign.type <- gsub(".*\\((.*)\\).*", "\\1", sign.syntax)
# repeat given the number ("remainder" from above)
sign.tokens <- rep(sign.type, remainder)
# collaps into character string
sign.tokens <- paste(sign.tokens, collapse = " ")
} else {
sign.tokens <- sign.syntax
}
signs[j] <- sign.tokens
}}) # end of try() function
# collapse the signs into one string
signs <- paste(signs, collapse = " _ ")
signs.orig <- paste(signs.orig, collapse = " _ ")
signs.orig.vec <- c(signs.orig.vec, signs.orig)
signs.expand.vec <- c(signs.expand.vec, signs)
}
# compile final data frame
cunei.data <- data.frame(tablet.id, signs.orig.vec, signs.expand.vec)
head(cunei.data)
head(cunei.data)
View(cunei.data)
cunei.data <- cunei.data[-which(cunei.data$signs.orig.vec == ""), ]
write.csv(cunei.data, file = paste("~/Github/UpperPalCombinatorics/data/CDLI/",
paste(period, "/signs_clean.csv", sep = ""),
sep =""), row.names = F)
knitr::opts_chunk$set(echo = TRUE)
library(stringr)
library(entropy)
library(gsubfn)
#library(devtools)
#install_github("dimalik/Hrate")
library(Hrate)
period <- "UrukIII"
# cuneiform data from CDLI
cunei.data <- read.csv(paste("~/Github/UpperPalCombinatorics/data/CDLI/",
paste(period, "/signs_clean.csv", sep = ""),
sep =""))
View(cunei.data)
View(cunei.data)
i
tablet.id <- cunei.data$tablet.id[i]
coding.sample <- as.character(cunei.data$signs.expand.vec[i])
coding.sample
i = 1
tablet.id <- cunei.data$tablet.id[i]
tablet.id
coding.sample <- as.character(cunei.data$signs.expand.vec[i])
coding.sample
chars <- unlist(strsplit(coding.sample, " "))
chars
num.char <- length(chars)
num.char
chars <- unlist(strsplit(cunei.data$signs.expand.vec, " "))
chars
chars <- strsplit(cunei.data$signs.expand.vec, " ")
View(chars)
num.char <- length(strsplit(cunei.data$signs.expand.vec, " "))
num.char
num.char <- lengths(strsplit(cunei.data$signs.expand.vec, " "))
num.char
head(cunei.data)
View(cunei.data)
cunei.data$num.char <- length(strsplit(cunei.data$signs.expand.vec, " "))
View(cunei.data)
cunei.data$num.char <- lengths(strsplit(cunei.data$signs.expand.vec, " "))
View(cunei.data)
knitr::opts_chunk$set(echo = TRUE)
sign.data <- read.csv("~/Github/UpperPalCombinatorics/data/signBase/signBase_randomized.csv")
View(sign.data)
