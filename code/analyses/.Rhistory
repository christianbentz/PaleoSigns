length.plot <- ggplot(estimations.df.selected, aes(x = log(num.char),
colour = subcorpus)) +
geom_density()
length.plot <- ggplot(estimations.df.selected, aes(x = log(num.char),
colour = subcorpus)) +
geom_density()
length.plot
mean(estimations.df.selected$subcorpus)
aggregate(estimations.df.selected$num.char~
estimations.df.selected$subcorpus, FUN = mean)
estimations.df <- read.csv("~/Github/UpperPalCombinatorics/results/features/features_combined.csv")
head(estimations.df)
subcorpus1 <- "Aurignacien"
subcorpus2 <- "Cuneiform (Uruk IV)"
selected <- c(subcorpus1, subcorpus2)
estimations.df <- estimations.df[estimations.df$subcorpus %in% selected, ]
View(estimations.df)
subcorpus1 <- "Aurignacien"
subcorpus2 <- "Cuneiform (Uruk V)"
selected <- c(subcorpus1, subcorpus2)
estimations.df <- estimations.df[estimations.df$subcorpus %in% selected, ]
length(estimations.df.selected)
nrow(estimations.df.selected)
aggregate(estimations.df.selected$subcorpus, FUN = nrow)
table(estimations.df.selected)
# number of rows (i.e. sequences) per subcorpus
table(estimations.df.selected$subcorpus)
estimations.df <- read.csv("~/Github/UpperPalCombinatorics/results/features/features_combined.csv")
head(estimations.df)
# choose subcorpora to compare: "Aurignacien", "TeDDi", "Cuneiform (Uruk V)",
# "Cuneiform (Uruk IV)", "Cuneiform (Uruk III)".
subcorpus1 <- "Aurignacien"
subcorpus2 <- "Cuneiform (Uruk V)"
selected <- c(subcorpus1, subcorpus2)
estimations.df <- estimations.df[estimations.df$subcorpus %in% selected, ]
estimations.subset <- estimations.df[c("subcorpus",
"huni.chars",
"hrate.chars",
"ttr.chars",
"rm.chars"
)]
estimations.subset <- na.omit(estimations.subset)
estimations.scaled <- cbind(estimations.subset["subcorpus"],
scale(estimations.subset[, c("huni.chars", "hrate.chars",
"ttr.chars", "rm.chars")]))
View(estimations.df)
estimations.df <- read.csv("~/Github/UpperPalCombinatorics/results/features/features_combined.csv")
head(estimations.df)
subcorpus1 <- "Aurignacien"
subcorpus2 <- "Cuneiform (Uruk V)"
selected <- c(subcorpus1, subcorpus2)
estimations.df <- estimations.df[estimations.df$subcorpus %in% selected, ]
View(estimations.df)
estimations.df <- read.csv("~/Github/UpperPalCombinatorics/results/features/features_combined.csv")
head(estimations.df)
# choose subcorpora to compare: "Aurignacien", "TeDDi", "Cuneiform (Uruk V)",
# "Cuneiform (Uruk IV)", "Cuneiform (Uruk III)".
subcorpus1 <- "Aurignacien"
subcorpus2 <- "Cuneiform (Uruk V)"
selected <- c(subcorpus1, subcorpus2)
estimations.df <- estimations.df[estimations.df$subcorpus %in% selected, ]
estimations.subset <- na.omit(estimations.subset)
estimations.scaled <- cbind(estimations.subset[, c("subcorpus", "sequence", "num.char")],
scale(estimations.subset[, c("huni.chars", "hrate.chars",
"ttr.chars", "rm.chars")]))
estimations.df <- read.csv("~/Github/UpperPalCombinatorics/results/features/features_combined.csv")
head(estimations.df)
View(estimations.df)
subcorpus1 <- "Aurignacien"
subcorpus2 <- "Cuneiform (Uruk V)"
selected <- c(subcorpus1, subcorpus2)
estimations.df <- estimations.df[estimations.df$subcorpus %in% selected, ]
View(estimations.df)
estimations.df <- read.csv("~/Github/UpperPalCombinatorics/results/features/features_combined.csv")
head(estimations.df)
subcorpus1 <- "Aurignacien"
subcorpus2 <- "Cuneiform (Uruk V)"
selected <- c(subcorpus1, subcorpus2)
estimations.subset <- estimations.df[estimations.df$subcorpus %in% selected, ]
estimations.subset <- na.omit(estimations.subset)
estimations.scaled <- cbind(estimations.subset[, c("subcorpus", "sequence", "num.char")],
scale(estimations.subset[, c("huni.chars", "hrate.chars",
"ttr.chars", "rm.chars")]))
View(estimations.scaled)
set.seed(1234)
# Randomly generating our training and test samples with a respective ratio of 2/3 and 1/3
datasample <- sample(2, nrow(estimations.scaled), replace = TRUE, prob = c(0.67, 0.33))
# Generate training set (select rows randomly by using datasample vector)
train <- estimations.scaled[datasample == 1, 1:ncol(estimations.scaled)]
# Generate test set
test <- estimations.scaled[datasample == 2, 1:ncol(estimations.scaled)]
View(test)
hidden.list <- list(c(4,4))
results.df <- data.frame(hidden.structure = character(0),
hidden.size = numeric(0), hidden.depth = numeric(0),
err.fct = character(0), act.fct.name = character(0),
algorithm = character(0), accuracy = numeric(0),
precision = numeric(0), recall = numeric(0),
f1 = numeric(0)
)
err.fct = 'sse' # options: 'sse', 'ce'
# choose activation function
act.fct = 'tanh' # options: 'logistic', 'tanh', softplus, relu,
# beware: the last two options have to be written without quotes ''
# note: softplus, relu, and tanh only work properly with sse as err.fct
act.fct.name = 'tanh' # make sure to give name as character string (for later storage)
# choose algorithm
algorithm = 'rprop+' # options: 'rprop+', 'rprop-', 'backprop', 'sag', 'slr'
# train classifier with neuralnet() function
try({ # if the processing failes for a certain file, there will be no output for this file,
# but the try() function allows the loop to keep running
classifier.mlp <- neuralnet(subcorpus == subcorpus1 ~ .,
data = train,
hidden = hidden,
threshold = 0.1, # defaults to 0.01
rep = 1, # number of reps in which new initial values are used,
# (essentially the same as a for loop)
stepmax = 100000, # defaults to 100K
linear.output = FALSE,
algorithm = algorithm, # defaults to "rprop+",
# i.e. resilient backpropagation
# learningrate = 0.1,
err.fct = err.fct,
act.fct = act.fct,
likelihood = TRUE,
lifesign = 'minimal')
classifier.mlp
# results matrix (each column represents one repetition)
classifier.mlp$result.matrix
# Prediction
# Get prediction using the predict() function
mlp.predictions <- predict(classifier.mlp, test,
rep = which.min(classifier.mlp$result.matrix[1,]), # Predict response values
# based on the "best" repetition (epoche), i.e. the one with the lowest error.
all.units = FALSE)
results.df <- data.frame(hidden.structure = character(0),
hidden.size = numeric(0), hidden.depth = numeric(0),
err.fct = character(0), act.fct.name = character(0),
algorithm = character(0), accuracy = numeric(0),
precision = numeric(0), recall = numeric(0),
f1 = numeric(0)
)
err.fct = 'sse' # options: 'sse', 'ce'
# choose activation function
act.fct = 'tanh' # options: 'logistic', 'tanh', softplus, relu,
# beware: the last two options have to be written without quotes ''
# note: softplus, relu, and tanh only work properly with sse as err.fct
act.fct.name = 'tanh' # make sure to give name as character string (for later storage)
# choose algorithm
algorithm = 'rprop+' # options: 'rprop+', 'rprop-', 'backprop', 'sag', 'slr'
classifier.mlp <- neuralnet(subcorpus == subcorpus1 ~ .,
data = train,
hidden = hidden,
threshold = 0.1, # defaults to 0.01
rep = 1, # number of reps in which new initial values are used,
# (essentially the same as a for loop)
stepmax = 100000, # defaults to 100K
linear.output = FALSE,
algorithm = algorithm, # defaults to "rprop+",
# i.e. resilient backpropagation
# learningrate = 0.1,
err.fct = err.fct,
act.fct = act.fct,
likelihood = TRUE,
lifesign = 'minimal')
hidden.list
hidden = c(4,4)
classifier.mlp <- neuralnet(subcorpus == subcorpus1 ~ .,
data = train,
hidden = hidden,
threshold = 0.1, # defaults to 0.01
rep = 1, # number of reps in which new initial values are used,
# (essentially the same as a for loop)
stepmax = 100000, # defaults to 100K
linear.output = FALSE,
algorithm = algorithm, # defaults to "rprop+",
# i.e. resilient backpropagation
# learningrate = 0.1,
err.fct = err.fct,
act.fct = act.fct,
likelihood = TRUE,
lifesign = 'minimal')
train
subcorpus1
subcorpus2
estimations.df <- read.csv("~/Github/UpperPalCombinatorics/results/features/features_combined.csv")
head(estimations.df)
subcorpus1 <- "Aurignacien"
subcorpus2 <- "Cuneiform (Uruk V)"
selected <- c(subcorpus1, subcorpus2)
estimations.subset <- estimations.df[estimations.df$subcorpus %in% selected, ]
estimations.subset <- na.omit(estimations.subset)
estimations.scaled <- cbind(estimations.subset[, c("subcorpus", "sequence", "num.char")],
scale(estimations.subset[, c("huni.chars", "hrate.chars",
"ttr.chars", "rm.chars")]))
set.seed(1234)
# Randomly generating our training and test samples with a respective ratio of 2/3 and 1/3
datasample <- sample(2, nrow(estimations.scaled), replace = TRUE, prob = c(0.67, 0.33))
# Generate training set (select rows randomly by using datasample vector)
train <- estimations.scaled[datasample == 1, 1:ncol(estimations.scaled)]
# Generate test set
test <- estimations.scaled[datasample == 2, 1:ncol(estimations.scaled)]
hidden <- c(4,4)
results.df <- data.frame(hidden.structure = character(0),
hidden.size = numeric(0), hidden.depth = numeric(0),
err.fct = character(0), act.fct.name = character(0),
algorithm = character(0), accuracy = numeric(0),
precision = numeric(0), recall = numeric(0),
f1 = numeric(0)
)
set.seed(123)
# counter
counter <- 0
# start time
start_time <- Sys.time()
err.fct = 'sse' # options: 'sse', 'ce'
# choose activation function
act.fct = 'tanh' # options: 'logistic', 'tanh', softplus, relu,
# beware: the last two options have to be written without quotes ''
# note: softplus, relu, and tanh only work properly with sse as err.fct
act.fct.name = 'tanh' # make sure to give name as character string (for later storage)
# choose algorithm
algorithm = 'rprop+' # options: 'rprop+', 'rprop-', 'backprop', 'sag', 'slr'
classifier.mlp <- neuralnet(subcorpus == subcorpus1 ~ .,
data = train,
hidden = hidden,
threshold = 0.1, # defaults to 0.01
rep = 1, # number of reps in which new initial values are used,
# (essentially the same as a for loop)
stepmax = 100000, # defaults to 100K
linear.output = FALSE,
algorithm = algorithm, # defaults to "rprop+",
# i.e. resilient backpropagation
# learningrate = 0.1,
err.fct = err.fct,
act.fct = act.fct,
likelihood = TRUE,
lifesign = 'minimal')
hidden <- c(4)
classifier.mlp <- neuralnet(subcorpus == subcorpus1 ~ .,
data = train,
hidden = hidden,
threshold = 0.1, # defaults to 0.01
rep = 1, # number of reps in which new initial values are used,
# (essentially the same as a for loop)
stepmax = 100000, # defaults to 100K
linear.output = FALSE,
algorithm = algorithm, # defaults to "rprop+",
# i.e. resilient backpropagation
# learningrate = 0.1,
err.fct = err.fct,
act.fct = act.fct,
likelihood = TRUE,
lifesign = 'minimal')
hidden <- list(c(4,4))
classifier.mlp <- neuralnet(subcorpus == subcorpus1 ~ .,
data = train,
hidden = hidden,
threshold = 0.1, # defaults to 0.01
rep = 1, # number of reps in which new initial values are used,
# (essentially the same as a for loop)
stepmax = 100000, # defaults to 100K
linear.output = FALSE,
algorithm = algorithm, # defaults to "rprop+",
# i.e. resilient backpropagation
# learningrate = 0.1,
err.fct = err.fct,
act.fct = act.fct,
likelihood = TRUE,
lifesign = 'minimal')
arch.no <- 100
# initialize empty list of hidden unit architectures
hidden.list <- vector(mode = "list", length = arch.no)
set.seed(09012020)
# run loop to generate number of layers and hidden units randomly
for (i in 1:arch.no) {
layer.no <- round(runif(1, min = 1, max = 4), 0)
hidden.units <- round(runif(layer.no, min = 1, max = 5), 0)
hidden.list[[i]] <- hidden.units
}
hidden.list
hidden.list[100]
hidden.list[5]
hidden <- hidden.list[5]
classifier.mlp <- neuralnet(subcorpus == subcorpus1 ~ .,
data = train,
hidden = hidden,
threshold = 0.1, # defaults to 0.01
rep = 1, # number of reps in which new initial values are used,
# (essentially the same as a for loop)
stepmax = 100000, # defaults to 100K
linear.output = FALSE,
algorithm = algorithm, # defaults to "rprop+",
# i.e. resilient backpropagation
# learningrate = 0.1,
err.fct = err.fct,
act.fct = act.fct,
likelihood = TRUE,
lifesign = 'minimal')
hidden <- hidden.list[6]
hidden
classifier.mlp <- neuralnet(subcorpus == subcorpus1 ~ .,
data = train,
hidden = hidden,
threshold = 0.1, # defaults to 0.01
rep = 1, # number of reps in which new initial values are used,
# (essentially the same as a for loop)
stepmax = 100000, # defaults to 100K
linear.output = FALSE,
algorithm = algorithm, # defaults to "rprop+",
# i.e. resilient backpropagation
# learningrate = 0.1,
err.fct = err.fct,
act.fct = act.fct,
likelihood = TRUE,
lifesign = 'minimal')
estimations.df <- read.csv("~/Github/UpperPalCombinatorics/results/features/features_combined.csv")
head(estimations.df)
subcorpus1 <- "Aurignacien"
subcorpus2 <- "Cuneiform (Uruk V)"
selected <- c(subcorpus1, subcorpus2)
estimations.subset <- estimations.df[estimations.df$subcorpus %in% selected, ]
estimations.subset <- na.omit(estimations.subset)
estimations.scaled <- cbind(estimations.subset[, c("subcorpus", "sequence", "num.char")],
scale(estimations.subset[, c("huni.chars", "hrate.chars",
"ttr.chars", "rm.chars")]))
set.seed(1234)
# Randomly generating our training and test samples with a respective ratio of 2/3 and 1/3
datasample <- sample(2, nrow(estimations.scaled), replace = TRUE, prob = c(0.67, 0.33))
# Generate training set (select rows randomly by using datasample vector)
train <- estimations.scaled[datasample == 1, 1:ncol(estimations.scaled)]
# Generate test set
test <- estimations.scaled[datasample == 2, 1:ncol(estimations.scaled)]
View(test)
View(train)
?neuralnet()
classifier.mlp <- neuralnet(subcorpus == subcorpus1 ~ .,
data = train[, c("huni.chars",
"hrate.chars",
"ttr.chars",
"rm.chars")],
hidden = hidden,
threshold = 0.1, # defaults to 0.01
rep = 1, # number of reps in which new initial values are used,
# (essentially the same as a for loop)
stepmax = 100000, # defaults to 100K
linear.output = FALSE,
algorithm = algorithm, # defaults to "rprop+",
# i.e. resilient backpropagation
# learningrate = 0.1,
err.fct = err.fct,
act.fct = act.fct,
likelihood = TRUE,
lifesign = 'minimal')
train[, c("huni.chars",
"hrate.chars",
"ttr.chars",
"rm.chars")]
train[, c("huni.chars",
"hrate.chars",
"ttr.chars",
"rm.chars")]
train[, c("huni.chars",
"hrate.chars",
"ttr.chars",
"rm.chars")]
train
hidden
classifier.mlp <- neuralnet(subcorpus == subcorpus1 ~ .,
data = train[, c("huni.chars",
"hrate.chars",
"ttr.chars",
"rm.chars")],
hidden = hidden,
threshold = 0.1, # defaults to 0.01
rep = 1, # number of reps in which new initial values are used,
# (essentially the same as a for loop)
stepmax = 100000, # defaults to 100K
linear.output = FALSE,
algorithm = algorithm, # defaults to "rprop+",
# i.e. resilient backpropagation
# learningrate = 0.1,
err.fct = err.fct,
act.fct = act.fct,
likelihood = TRUE,
lifesign = 'minimal')
classifier.mlp <- neuralnet(subcorpus == subcorpus1 ~ .,
data = train[, c("subcorpus",
"huni.chars",
"hrate.chars",
"ttr.chars",
"rm.chars")],
hidden = hidden,
threshold = 0.1, # defaults to 0.01
rep = 1, # number of reps in which new initial values are used,
# (essentially the same as a for loop)
stepmax = 100000, # defaults to 100K
linear.output = FALSE,
algorithm = algorithm, # defaults to "rprop+",
# i.e. resilient backpropagation
# learningrate = 0.1,
err.fct = err.fct,
act.fct = act.fct,
likelihood = TRUE,
lifesign = 'minimal')
hidden <- c(4,4)
classifier.mlp <- neuralnet(subcorpus == subcorpus1 ~ .,
data = train[, c("subcorpus",
"huni.chars",
"hrate.chars",
"ttr.chars",
"rm.chars")],
hidden = hidden,
threshold = 0.1, # defaults to 0.01
rep = 1, # number of reps in which new initial values are used,
# (essentially the same as a for loop)
stepmax = 100000, # defaults to 100K
linear.output = FALSE,
algorithm = algorithm, # defaults to "rprop+",
# i.e. resilient backpropagation
# learningrate = 0.1,
err.fct = err.fct,
act.fct = act.fct,
likelihood = TRUE,
lifesign = 'minimal')
classifier.mlp
# results matrix (each column represents one repetition)
classifier.mlp$result.matrix
mlp.predictions <- predict(classifier.mlp, test,
rep = which.min(classifier.mlp$result.matrix[1,]), # Predict response values
# based on the "best" repetition (epoche), i.e. the one with the lowest error.
all.units = FALSE)
mlp.predictions
mlp.predictions.rd <- ifelse(mlp.predictions > 0.5, subcorpus1, subcorpus2)
mlp.predictions.rd
test.labels <- data.frame(test$subcorpus)
View(test.labels)
class.comparison <- data.frame(mlp.predictions.rd, test.labels)
# giving appropriate column names
names(class.comparison) <- c("predicted", "observed")
# inspecting our results table
head(class.comparison)
# get confusion matrix
cm <- confusionMatrix(as.factor(class.comparison$predicted),
reference = as.factor(class.comparison$observed))
accuracy <- cm$overall['Accuracy']
f1 <- cm[["byClass"]]["F1"]
recall <- cm[["byClass"]]["Recall"]
precision <- cm[["byClass"]]["Precision"]
# unname and round resulting values
accuracy <- round(unname(accuracy), 2)
f1 <- round(unname(f1), 2)
recall <- round(unname(recall), 2)
precision <- round(unname(precision), 2)
# append results to dataframe
hidden.structure <- paste(unlist(hidden), collapse = ",")
hidden.size <- sum(unlist(hidden))
hidden.depth <- length(hidden)
local.df <- data.frame(hidden.structure, hidden.size,
hidden.depth, err.fct, act.fct.name, algorithm,
accuracy, precision, recall, f1)
results.df <- rbind(results.df, local.df)
results.df
mlp.predictions.rd
mlp.predictions.df <- cbind(test, mlp.predictions.rd)
View(mlp.predictions.df)
classifier.mlp
estimations.df <- read.csv("~/Github/UpperPalCombinatorics/results/features/features_combined.csv")
head(estimations.df)
subcorpus1 <- "Aurignacien"
subcorpus2 <- "Cuneiform (Uruk V)"
selected <- c(subcorpus1, subcorpus2)
estimations.subset <- estimations.df[estimations.df$subcorpus %in% selected, ]
estimations.subset <- na.omit(estimations.subset)
estimations.scaled <- cbind(estimations.subset[, c("subcorpus", "sequence", "num.char")],
scale(estimations.subset[, c("huni.chars", "hrate.chars",
"ttr.chars", "rm.chars")]))
set.seed(1234)
# Randomly generating our training and test samples with a respective ratio of 2/3 and 1/3
datasample <- sample(2, nrow(estimations.scaled), replace = TRUE, prob = c(0.67, 0.33))
# Generate training set (select rows randomly by using datasample vector)
train <- estimations.scaled[datasample == 1, 1:ncol(estimations.scaled)]
# Generate test set
test <- estimations.scaled[datasample == 2, 1:ncol(estimations.scaled)]
View(test)
View(test)
?confusionMatrix()
cm
baseline <- cm$overall['No Information Rate']
baseline
cm$overall
baseline <- cm$overall['AccuracyNull']
accuracy <- cm$overall['Accuracy']
accuracy.pvalue <- cm$overall['AccuracyPValue']
mcnemar.pvalue <- cm$overall['McnemarPValue']
f1 <- cm[["byClass"]]["F1"]
recall <- cm[["byClass"]]["Recall"]
precision <- cm[["byClass"]]["Precision"]
baseline <- round(unname(baseline), 2)
accuracy <- round(unname(accuracy), 2)
accuracy.pvalue <- round(unname(accuracy.pvalue), 2)
mcnemar.pvalue <- round(unname(mcnemar.pvalue), 2)
f1 <- round(unname(f1), 2)
recall <- round(unname(recall), 2)
precision <- round(unname(precision), 2)
?confusionMatrix()
file.choose()
knitr::opts_chunk$set(echo = TRUE)
performance.mlp <- read.csv("~/Github/UpperPalCombinatorics/results/classifier/MLP/performance/performance_MLP_tanh_sse_rprop+_Aurignacien_Cuneiform (Uruk V)_.csv")
performance.mlp <- read.csv("~/Github/UpperPalCombinatorics/results/classifier/MLP/performance/performance_MLP_tanh_sse_rprop+_Aurignacien_Cuneiform (Uruk V)_.csv")
head(estimations.df)
View(performance.mlp)
